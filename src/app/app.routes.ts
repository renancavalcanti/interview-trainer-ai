import { Routes } from '@angular/router';
import { InterviewChatComponent } from './interview-chat/interview-chat.component';

export const routes: Routes = [
    { path: 'interview', component: InterviewChatComponent },

  ];